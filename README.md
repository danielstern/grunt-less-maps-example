grunt-less-maps-example
=======================

This simple example has working LESS maps.

To run, clone the repository and run "grunt."

It should open in chrome with a correctly configured source map set up.
